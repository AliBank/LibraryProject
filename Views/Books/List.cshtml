@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<LibraryAppWeb.Models.Entities.Book>

<h1>All Books</h1>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Published Year</th>
            <th>Genre</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.PublishedYear</td>
                <td>@book.Genre</td>
                <td>
                    <a class="btn btn-primary" asp-controller="Books" asp-action="Edit" asp-route-id="@book.BookId">Edit</a>
                    <form asp-controller="Books" asp-action="Delete" asp-route-id="@book.BookId" method="post" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this book?');">
                        <button type="submit" class="btn btn-danger ms-3">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

